#include <iostream>
using namespace std;

int main() {
  int a, b, c, sum;
  cin >> c;
  for (int i = 1; i <= c; i++)
  {
    cin >> a >> b;
    printf("Case #%d: %d\n", i, a + b);
  }
  
  
}
